# Manual tasks

Per `docs/CLAUDE-CODE-PROMPT.md` T6.2 — things that need Jack in an admin panel or making a business call, not more code. Nothing in this list was attempted by any coding session; each is a genuine "someone has to actually do this" item.

1. **Shopify admin → Settings → Checkout → Customize:** set logo, colours, and font from the checkout editor's own list to match `BRAND.md`'s palette/type as closely as the editor allows. Non-Plus store: no custom CSS is possible — this is a manual, admin-only match, not something a deploy can automate.
2. **Verify the thank-you and order-status pages are on the current checkout**, not a legacy customisation — `docs/COMMERCE-ARCHITECTURE.md` notes an August 2026 migration deadline for this; confirm it's actually done in the live store.
3. **Add product metafields in Shopify** for all 8 SKUs: `mil`, `capacityCc`, `dimensions`, `foodSafeCert`, `bagSizeFits`, `whoFor`, `whoNotFor`, `notTested`. The site-side equivalent (`src/lib/commerce.ts`'s `specs` field on `CatalogProduct`) is already populated for the facts the existing catalog copy already states — this item is about the *Shopify-side* source of truth catching up, particularly `foodSafeCert`, which no catalog copy anywhere currently states a value for (see `docs/images.md` and `docs/components.md`'s `SpecSheet` entry — it renders "—" for this today, truthfully).
4. **Verify every row of `src/data/sizing.json`** — this file does not exist yet (T3.13's absorber/batch sizing calculators are the pre-existing `src/pages/tools/absorber-calculator.astro` / `batch-planner.astro`, which already have their own sourced data modules under `src/data/` — `absorberSizing.ts`, `batchPlanning.ts`. Review *those* against your own tests instead; the file name in the original spec doesn't match what's actually shipped).
5. **Fill every `TODO(jack)` marker.** Run this to find them all:
   ```sh
   grep -rn "TODO(jack)" src/
   ```
   Verified this session: zero matches in `src/` as of the last commit on this branch. (`grep -rn "TODO(jack)" src/ docs/` finds matches, but every one of them is inside `docs/CLAUDE-CODE-PROMPT.md`'s own instructional text *describing* the convention, not an actual marker anyone left in content or code — scope the grep to `src/` only, or you'll be chasing the spec document's own prose.) Re-run this yourself before relying on it; content changes independently of this branch.
6. **Set the Storefront API token and newsletter endpoint in Netlify env.** No Shopify Storefront credentials exist in any environment this session ran in; the site has been building and testing against the deterministic "unstocked"/fail-closed fallback the whole time (`src/lib/stock.ts`), which is documented, correct behaviour without credentials — not a bug, but also not a substitute for verifying the real, live cart/checkout flow works end to end with real credentials.
7. **Add Shopify selling plans if subscribe-and-save is wanted** for absorbers/bags. Not evaluated this session — check whether selling plans already exist on any variant before assuming they don't; if they do, the store index (`src/pages/shop.astro`) doesn't currently surface a subscribe option in its variant selection.
8. **Review `docs/screens/`** (gitignored — run `node scripts/static-server.mjs` then `node scripts/screenshot-templates.mjs` to regenerate) before merging any branch that touches layout, tokens, or a shared component. This session's own T5.5 pass found three real bugs (a missing-image accessibility violation, a colour-contrast regression, and a test-infrastructure bug that was silently hiding both) purely by looking at the screenshots rather than trusting automated checks alone — automated checks (axe, Lighthouse) did NOT catch two of those three on their own.
9. **Decide the design-supersession question this session flagged and got resolved, but which has a downstream consequence not yet acted on:** `docs/ART-DIRECTION.md` was retired per the 2026-09-10 supersession decision, but its exact shot-list file paths (`public/images/hero.jpg` and the rest — full list in `docs/DECISIONS.md`) are still what the live placeholder system watches for. If real photography is commissioned, decide whether to keep shooting to that exact list or supersede it with a new one under `docs/design-research.md` §2's manifest-driven approach (`src/data/images.json`, still not built — see `docs/images.md`).
10. **The methodology decision has a downstream content-review implication:** every already-published guide/comparison should be spot-checked against the "Sublime Pantry does no physical testing" position (`docs/DECISIONS.md`'s methodology decision) — this session fixed the *components being built this session* to comply, and confirmed `/review-methodology` already states the position correctly, but did not do a full sweep of every existing published article's prose for language that might imply hands-on testing outside what `tests/claim-shapes.test.mjs`'s regex already catches.
